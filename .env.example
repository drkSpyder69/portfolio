### Example env file for Vite — copy to .env or set in CI
# Set this to your production site origin (no trailing slash), e.g. https://example.com
VITE_SITE_URL=https://aqibjawwad.me

# After setting, build the site so Vite replaces %VITE_SITE_URL% in index.html